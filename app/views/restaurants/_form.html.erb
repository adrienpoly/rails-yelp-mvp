 <%= simple_form_for @restaurant do |f| %>
    <%= f.input :name %>
    <%= f.input :address %>
    <%= f.input :phone_number %>
    <%= f.input :category, collection: @restaurant.categories, selected: "french" %>
    <%= f.submit "add a new restaurant", class: "btn btn-primary" %>
  <% end %>
